# Copy this to .env.local for local dev, or set it directly in
# Vercel -> Project -> Settings -> Environment Variables for production.
#
# Get a free key at https://aistudio.google.com/apikey (no credit card
# required -- the Gemini 2.5 Flash free tier is used by api/chat.js).
GEMINI_API_KEY=
